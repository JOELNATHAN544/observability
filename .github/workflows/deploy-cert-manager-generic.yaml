# Automated deployment of cert-manager to Generic Kubernetes Cluster
# Designed for local clusters (kind, k3s, minikube) or any Kubernetes cluster with kubeconfig
# Uses Kubernetes backend for state storage (Secret in kube-system namespace)
# Perfect for development, testing, and non-cloud deployments
name: Deploy cert-manager (Generic K8s)

on:
  # Manual trigger with plan/apply option
  workflow_dispatch:
    inputs:
      terraform_action:
        description: 'Terraform Action'
        required: true
        type: choice
        options:
          - plan
          - apply
        default: 'plan'
      adopt_existing_resources:
        description: 'Import existing cert-manager resources into Terraform state'
        required: false
        type: boolean
        default: false
  # Auto-trigger on push to main branch
  push:
    branches:
      - main
    paths:
      - 'cert-manager/terraform/**'
      - '.github/workflows/deploy-cert-manager-generic.yaml'

# Required permissions for workflow operations
permissions:
  contents: read           # Read repository content

# Global environment variables used across all jobs
env:
  TERRAFORM_VERSION: '1.6.0'
  KUBECTL_VERSION: '1.28.0'
  WORKING_DIR: 'cert-manager/terraform'
  CLOUD_PROVIDER: 'generic'

jobs:
  # Job 1: Generate and review Terraform infrastructure plan
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    outputs:
      plan_exitcode: ${{ steps.plan.outputs.exitcode }}
    
    steps:
      # Fetch repository code
      - name: Checkout code
        uses: actions/checkout@v4

      # Install kubectl for Kubernetes backend and provider
      - name: Setup kubectl
        run: |
          curl -LO "https://dl.k8s.io/release/v${KUBECTL_VERSION}/bin/linux/amd64/kubectl"
          chmod +x kubectl
          sudo mv kubectl /usr/local/bin/
          
          # Decode kubeconfig from GitHub Secret (base64 encoded)
          # To create: cat ~/.kube/config | base64 -w 0
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > ~/.kube/config
          
          # Test cluster connectivity
          kubectl cluster-info
          kubectl get nodes
          echo "✅ Connected to Kubernetes cluster"

      # Install Terraform CLI
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      # Remove stale backend configuration files
      # IMPORTANT: State Secrets in kube-system are NEVER deleted - only local config regenerated
      - name: Cleanup old backend configs
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          rm -f backend.tf backend-config.tf

      # Generate backend-config.tf for Kubernetes backend
      # State stored as Secret: tfstate-default-cert-manager in kube-system namespace
      - name: Configure backend
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          bash ../../.github/scripts/configure-backend.sh "${{ env.CLOUD_PROVIDER }}" "cert-manager"

      # Initialize Terraform: download providers, configure backend, load state from Secret
      - name: Terraform Init
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform init


      # Import existing resources (optional)
      - name: Import Existing Resources
        if: ${{ inputs.adopt_existing_resources == true }}
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          echo "════════════════════════════════════════════════════════════════"
          echo "Checking for existing cert-manager resources to import..."
          echo "════════════════════════════════════════════════════════════════"
          
          # Check if Helm release exists
          if helm list -n cert-manager 2>/dev/null | grep -q "cert-manager"; then
            echo "✓ Found existing Helm release: cert-manager"
            echo "Importing into Terraform state..."
            terraform import 'helm_release.cert_manager[0]' cert-manager/cert-manager || echo "⚠ Already in state or import failed (will continue)"
          else
            echo "ℹ No existing cert-manager Helm release found"
          fi
          
          echo "════════════════════════════════════════════════════════════════"
          echo "Import check complete! Proceeding with Terraform operations..."
          echo "════════════════════════════════════════════════════════════════"

      # Validate Terraform syntax and configuration
      - name: Terraform Validate
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform validate

      # Generate terraform.tfvars with deployment configuration
      - name: Create terraform.tfvars
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          cat > terraform.tfvars <<EOF
          cloud_provider       = "${{ env.CLOUD_PROVIDER }}"
          install_cert_manager = true
          create_issuer        = true
          release_name         = "cert-manager"
          cert_manager_version = "v1.19.2"
          namespace            = "cert-manager"
          letsencrypt_email    = "${{ secrets.LETSENCRYPT_EMAIL }}"
          cert_issuer_name     = "letsencrypt-prod"
          cert_issuer_kind     = "ClusterIssuer"
          issuer_server        = "https://acme-v02.api.letsencrypt.org/directory"
          ingress_class_name   = "nginx"
          EOF

      # Execute Terraform plan and capture exit code
      # Exit codes: 0 = no changes, 1 = error, 2 = changes detected
      - name: Terraform Plan
        id: plan
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          set +e
          terraform plan -out=tfplan -detailed-exitcode
          EXIT_CODE=$?
          set -e
          
          echo "exitcode=$EXIT_CODE" >> $GITHUB_OUTPUT
          
          if [ $EXIT_CODE -eq 1 ]; then
            echo "❌ Terraform plan failed!"
            exit 1
          fi
          
          terraform show tfplan > plan.txt
          exit 0

      # Save plan artifacts for apply job
      - name: Upload plan
        uses: actions/upload-artifact@v4
        with:
          name: tfplan-cert-manager-generic
          path: |
            ${{ env.WORKING_DIR }}/tfplan
            ${{ env.WORKING_DIR }}/plan.txt
          retention-days: 5

  # Job 2: Apply Terraform plan and verify deployment
  terraform-apply-and-verify:
    name: Terraform Apply & Verify
    runs-on: ubuntu-latest
    needs: [terraform-plan]
    if: |
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.terraform_action == 'apply')
    environment:
      name: production
    
    steps:
      # Fetch repository code
      - name: Checkout code
        uses: actions/checkout@v4

      # Install Terraform CLI
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      # Install kubectl and configure cluster access
      - name: Setup kubectl
        run: |
          curl -LO "https://dl.k8s.io/release/v${KUBECTL_VERSION}/bin/linux/amd64/kubectl"
          chmod +x kubectl
          sudo mv kubectl /usr/local/bin/
          
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > ~/.kube/config
          
          kubectl cluster-info
          echo "✅ Connected to cluster"

      # Recreate terraform.tfvars (must match plan job)
      - name: Create terraform.tfvars
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          cat > terraform.tfvars <<EOF
          cloud_provider       = "${{ env.CLOUD_PROVIDER }}"
          install_cert_manager = true
          create_issuer        = true
          release_name         = "cert-manager"
          cert_manager_version = "v1.19.2"
          namespace            = "cert-manager"
          letsencrypt_email    = "${{ secrets.LETSENCRYPT_EMAIL }}"
          cert_issuer_name     = "letsencrypt-prod"
          cert_issuer_kind     = "ClusterIssuer"
          issuer_server        = "https://acme-v02.api.letsencrypt.org/directory"
          ingress_class_name   = "nginx"
          EOF

      # Remove stale backend configs
      - name: Cleanup old backend configs
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          rm -f backend.tf backend-config.tf

      # Regenerate backend configuration
      - name: Configure backend
        working-directory: ${{ env.WORKING_DIR }}
        run: |
          bash ../../.github/scripts/configure-backend.sh "${{ env.CLOUD_PROVIDER }}" "cert-manager"

      # Re-initialize Terraform
      - name: Terraform Init
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform init

      # Download the plan artifact
      - name: Download plan
        uses: actions/download-artifact@v4
        with:
          name: tfplan-cert-manager-generic
          path: ${{ env.WORKING_DIR }}

      # Execute the plan to deploy cert-manager
      # Updates state in Kubernetes Secret: kube-system/tfstate-default-cert-manager
      - name: Terraform Apply
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform apply -auto-approve tfplan

      # Extract Terraform outputs
      - name: Output deployment info
        working-directory: ${{ env.WORKING_DIR }}
        run: terraform output -json > terraform-outputs.json

      # Save outputs as artifact
      - name: Upload outputs
        uses: actions/upload-artifact@v4
        with:
          name: terraform-outputs-cert-manager-generic
          path: ${{ env.WORKING_DIR }}/terraform-outputs.json
          retention-days: 30

      # Verify successful deployment
      - name: Verify cert-manager deployment
        run: |
          echo "🔍 Verifying cert-manager deployment..."
          
          # Check namespace
          echo "Checking cert-manager namespace..."
          kubectl get namespace cert-manager
          
          # Check pods
          echo "Checking cert-manager pods..."
          kubectl get pods -n cert-manager
          kubectl wait --for=condition=ready pod -l app.kubernetes.io/instance=cert-manager -n cert-manager --timeout=300s
          
          # Verify ClusterIssuer
          echo "Checking ClusterIssuer..."
          kubectl get clusterissuer letsencrypt-prod
          kubectl describe clusterissuer letsencrypt-prod
          
          echo "✅ cert-manager deployment verified successfully"
