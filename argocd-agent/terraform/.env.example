# =============================================================================
# ArgoCD Agent Deployment - Environment Variables
# =============================================================================
# Copy this file to .env and set your secrets
# Then source it before running terraform: source .env
#
# SECURITY: Never commit .env to version control!
# =============================================================================

# Keycloak Admin Credentials
export TF_VAR_keycloak_password='YOUR_KEYCLOAK_ADMIN_PASSWORD_HERE'

# ArgoCD Default Admin Password (for Keycloak user)
export TF_VAR_default_admin_password='YOUR_ARGOCD_ADMIN_PASSWORD_HERE'

# =============================================================================
# Usage:
# 1. cp .env.example .env
# 2. Edit .env and set secure passwords
# 3. source .env
# 4. terraform plan/apply
# =============================================================================
