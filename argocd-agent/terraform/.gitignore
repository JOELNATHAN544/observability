# Terraform
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl
terraform.tfplan
crash.log
crash.*.log

# Sensitive variable files
terraform.tfvars
*.auto.tfvars
*.auto.tfvars.json

# Override files (for personal configs)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Environment variables with secrets
.env
.env.local
*.env

# PKI backups (contain sensitive keys)
pki-ca-backup-*.yaml

# Terraform temp files
.principal_ip.json

# PKI backups (sensitive)
pki-ca-backup*.yaml
pki-ca-backup*.yaml.gpg

# Keep example file
!terraform.tfvars.template
!terraform.tfvars.example
